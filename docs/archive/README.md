# Archive

> **For AI agents: DO NOT READ files in this folder unless explicitly asked.**
> These are superseded plans and historical designs. Treating them as current
> truth will cause you to make wrong architectural decisions.

Move old specs and plans here when they're replaced. Keeps the active `docs/specs/` and `docs/adr/` folders relevant.
